apiVersion: v1
kind: Service
metadata:
  name: developeriq-service
  labels:
    app: developeriq
spec:
  selector:
    app: developeriq
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer
